import React from 'react'

const Loading = (props) => {
    return (
        <div className="prompt">
            <i class="fas fa-spinner fa-pulse"></i>
        </div>
    )
}

export default Loading
